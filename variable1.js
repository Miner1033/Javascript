console.log("miner");

// 🔹 What is a Variable in JavaScript?
// A variable is like a container used to store data (like numbers, strings, etc.) that you can use and change later in your program.

// Think of it like a box where you store a value. You give the box a name, so you can find and use the value later.

// 🔹 How to Declare a Variable?
// You can declare a variable using these keywords:

// Keyword	Description
// var	Old way of declaring variables. Has function scope.
// let	Modern way. Has block scope (recommended).
// const	Used for values that don't change.
let name="rimon";
const pi=3.14;
var age=20;
console.log(name);
    console.log(pi);
console.log(age);
// Output: rimon
let n=20;
let m=30;
let sum=n+m;
console.log(sum);